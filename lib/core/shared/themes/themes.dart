import 'package:flutter/material.dart';

class themes{
  static ThemeData darktheme=ThemeData(
    scaffoldBackgroundColor: Color.fromRGBO(16, 11, 32, 1.0),
    textTheme: TextTheme(
      bodyLarge: TextStyle(
        color: Colors.white,
        fontSize: 15,
      )
    )
  );
}